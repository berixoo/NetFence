<UserControl x:Class="NetFence.App.Pages.ScanBlockPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource ContentBackground}" Margin="16">
        <Grid.Resources>
            <Style TargetType="Button">
                <Style.Triggers>
                    <Trigger Property="IsEnabled" Value="False">
                        <Setter Property="Opacity" Value="0.6"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Target path input + Select EXE/Select Folder buttons -->
        <Grid Grid.Row="0" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="96"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="TargetPathLabel" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,10,6"
                       Foreground="{DynamicResource PrimaryText}"/>
            <TextBox x:Name="PathBox" Grid.Column="1" Height="30" VerticalContentAlignment="Center" Margin="0,4,8,4"
                     Background="{DynamicResource InputBackground}" Foreground="{DynamicResource PrimaryText}"
                     BorderBrush="{DynamicResource BorderColor}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="SelectExeButton" MinWidth="104" Height="32" Padding="12,0" Margin="4"
                        Background="{DynamicResource ButtonSecondaryBackground}"
                        Foreground="{DynamicResource ButtonSecondaryForeground}"
                        BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                        Click="SelectExeButton_Click"/>
                <Button x:Name="SelectFolderButton" MinWidth="116" Height="32" Padding="12,0" Margin="4"
                        Background="{DynamicResource ButtonSecondaryBackground}"
                        Foreground="{DynamicResource ButtonSecondaryForeground}"
                        BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                        Click="SelectFolderButton_Click"/>
            </StackPanel>
        </Grid>

        <!-- Row 1: Rule name input + "Include child processes" checkbox -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="96"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="RuleNameLabel" Grid.Column="0" VerticalAlignment="Center" Margin="0,6,10,6"
                       Foreground="{DynamicResource PrimaryText}"/>
            <TextBox x:Name="NameBox" Grid.Column="1" Height="30" VerticalContentAlignment="Center" Margin="0,4,8,4"
                     Background="{DynamicResource InputBackground}" Foreground="{DynamicResource PrimaryText}"
                     BorderBrush="{DynamicResource BorderColor}"/>
            <CheckBox x:Name="IncludeLinkedCheck" Grid.Column="2" Margin="8,0,4,0" IsChecked="True"
                      Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center"/>
        </Grid>

        <!-- Row 2: Action buttons -->
        <WrapPanel Grid.Row="2" Margin="0,4,0,10">
            <Button x:Name="ScanButton" MinWidth="124" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonPrimaryBackground}"
                    Foreground="{DynamicResource ButtonPrimaryForeground}"
                    Click="ScanButton_Click"/>
            <Button x:Name="BlockButton" MinWidth="124" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonDangerBackground}"
                    Foreground="{DynamicResource ButtonDangerForeground}"
                    Click="BlockButton_Click"/>
            <Button x:Name="UnblockButton" MinWidth="112" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonSuccessBackground}"
                    Foreground="{DynamicResource ButtonSuccessForeground}"
                    Click="UnblockButton_Click"/>
            <Button x:Name="UnblockSelectedButton" MinWidth="128" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                    Click="UnblockSelectedButton_Click"/>
            <Button x:Name="UnblockAllButton" MinWidth="118" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                    Click="UnblockAllButton_Click"/>
            <Button x:Name="ExportButton" MinWidth="132" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                    Click="ExportButton_Click"/>
            <Button x:Name="OpenLogButton" MinWidth="112" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                    Click="OpenLogButton_Click"/>
            <Button x:Name="RefreshButton" MinWidth="104" Height="32" Padding="12,0" Margin="4"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"
                    Click="RefreshButton_Click"/>
        </WrapPanel>

        <!-- Row 3: TabControl with two tabs -->
        <TabControl x:Name="Tabs" Grid.Row="3" Margin="0,0,0,10"
                    Background="Transparent"
                    BorderBrush="{DynamicResource BorderColor}">
            <TabControl.Resources>
                <Style TargetType="TabPanel">
                    <Setter Property="Background" Value="{DynamicResource PanelBackground}"/>
                </Style>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TabInactiveText}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="14,7"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="TabBorder" Background="Transparent"
                                        BorderBrush="Transparent" BorderThickness="0,0,0,2"
                                        Padding="{TemplateBinding Padding}" Cursor="Hand">
                                    <ContentPresenter x:Name="TabContent"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="TabBorder" Property="BorderBrush"
                                                Value="{DynamicResource TabSelectedUnderline}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                        <Setter TargetName="TabContent" Property="TextElement.FontWeight"
                                                Value="SemiBold"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem x:Name="CandidatesTab">
                <TabItem.Header>
                    <TextBlock x:Name="CandidatesTabHeader" Foreground="{DynamicResource PrimaryText}"/>
                </TabItem.Header>
                <DataGrid x:Name="CandidatesGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                          IsReadOnly="False"
                          Background="{DynamicResource ContentBackground}"
                          Foreground="{DynamicResource PrimaryText}"
                          BorderBrush="{DynamicResource BorderColor}"
                          AlternatingRowBackground="{DynamicResource GridRowAlt}"
                          ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                    <DataGrid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn x:Name="CandidateBlockColumn" Binding="{Binding Selected}" Width="60"/>
                        <DataGridTextColumn x:Name="CandidateProgramColumn" Binding="{Binding Program}" IsReadOnly="True" Width="3*"/>
                        <DataGridTextColumn x:Name="CandidateReasonColumn" Binding="{Binding Reason}" IsReadOnly="True" Width="2*"/>
                        <DataGridTextColumn x:Name="CandidatePidColumn" Binding="{Binding ProcessId}" IsReadOnly="True" Width="60"/>
                        <DataGridTextColumn x:Name="CandidateProcessColumn" Binding="{Binding ProcessName}" IsReadOnly="True" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem x:Name="RulesTab">
                <TabItem.Header>
                    <TextBlock x:Name="RulesTabHeader" Foreground="{DynamicResource PrimaryText}"/>
                </TabItem.Header>
                <DataGrid x:Name="RulesGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                          IsReadOnly="True" SelectionMode="Extended" SelectionUnit="FullRow"
                          Background="{DynamicResource ContentBackground}"
                          Foreground="{DynamicResource PrimaryText}"
                          BorderBrush="{DynamicResource BorderColor}"
                          AlternatingRowBackground="{DynamicResource GridRowAlt}"
                          ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
                    <DataGrid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="RuleProfileColumn" Binding="{Binding ProfileName}" Width="140"/>
                        <DataGridTextColumn x:Name="RuleDirectionColumn" Binding="{Binding Direction}" Width="80"/>
                        <DataGridTextColumn x:Name="RuleEnabledColumn" Binding="{Binding Enabled}" Width="70"/>
                        <DataGridTextColumn x:Name="RuleActionColumn" Binding="{Binding Action}" Width="70"/>
                        <DataGridTextColumn x:Name="RuleProgramColumn" Binding="{Binding Program}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- Row 4: Progress + Status -->
        <StackPanel Grid.Row="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ProgressBar x:Name="Progress" Height="4" Margin="0,0,0,6" IsIndeterminate="True" Visibility="Collapsed"
                             Foreground="{DynamicResource ProgressBarForeground}"/>
                <Button x:Name="CancelButton" Grid.Column="1" Width="70" Height="24"
                        Visibility="Collapsed" Click="CancelButton_Click"
                        Background="{DynamicResource ButtonDangerBackground}"
                        Foreground="{DynamicResource ButtonDangerForeground}" BorderThickness="0"
                        FontSize="11" Margin="8,0,0,6"/>
            </Grid>
            <TextBlock x:Name="StatusText" TextWrapping="Wrap" Foreground="{DynamicResource PrimaryText}"/>
        </StackPanel>
    </Grid>
</UserControl>
