<UserControl x:Class="NetFence.App.Pages.RuleProfilesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="16" Background="{DynamicResource ContentBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Profile section header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock x:Name="ProfilesLabel" FontSize="14" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center"/>
            <Button x:Name="SaveProfileButton" MinWidth="80" Height="28" Margin="8,0,4,0"
                    Click="SaveProfileButton_Click"
                    Background="{DynamicResource ButtonPrimaryBackground}"
                    Foreground="{DynamicResource ButtonPrimaryForeground}" BorderThickness="0"/>
            <Button x:Name="LoadProfileButton" MinWidth="90" Height="28" Margin="4,0"
                    Click="LoadProfileButton_Click"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"/>
            <Button x:Name="DeleteProfileButton" MinWidth="60" Height="28" Margin="4,0"
                    Click="DeleteProfileButton_Click"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"/>
            <Button x:Name="ExportProfileButton" MinWidth="80" Height="28" Margin="4,0"
                    Click="ExportProfileButton_Click"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"/>
            <Button x:Name="ImportProfileButton" MinWidth="80" Height="28" Margin="4,0"
                    Click="ImportProfileButton_Click"
                    Background="{DynamicResource ButtonSecondaryBackground}"
                    Foreground="{DynamicResource ButtonSecondaryForeground}"
                    BorderBrush="{DynamicResource ButtonSecondaryBorder}"/>
        </StackPanel>

        <!-- Mode selector -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock x:Name="ModeLabel" VerticalAlignment="Center"
                       Foreground="{DynamicResource PrimaryText}" Margin="0,0,6,0"/>
            <ComboBox x:Name="ModeBox" Width="160" Height="28"
                      SelectionChanged="ModeBox_SelectionChanged">
                <ComboBoxItem x:Name="ModeBlockAll" Tag="block_all"/>
                <ComboBoxItem x:Name="ModeAllowAll" Tag="allow_all"/>
                <ComboBoxItem x:Name="ModeLanOnly" Tag="lan_only"/>
                <ComboBoxItem x:Name="ModeCustom" Tag="custom"/>
            </ComboBox>
            <Button x:Name="ApplyModeButton" MinWidth="90" Height="28" Margin="8,0,0,0"
                    Click="ApplyModeButton_Click"
                    Background="{DynamicResource ButtonPrimaryBackground}"
                    Foreground="{DynamicResource ButtonPrimaryForeground}" BorderThickness="0"/>
        </StackPanel>

        <!-- Custom IP/Domain editor -->
        <StackPanel x:Name="CustomIpPanel" Grid.Row="2" Visibility="Collapsed" Margin="0,0,0,8">
            <TextBlock x:Name="AllowedIpsLabel" Margin="0,0,0,4"
                       Foreground="{DynamicResource PrimaryText}"/>
            <TextBox x:Name="AllowedIpsBox" Height="70" MinHeight="50"
                     TextWrapping="Wrap" AcceptsReturn="True"
                     Background="{DynamicResource InputBackground}"
                     Foreground="{DynamicResource PrimaryText}"
                     BorderBrush="{DynamicResource BorderColor}"
                     VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Profiles DataGrid -->
        <DataGrid x:Name="ProfilesGrid" Grid.Row="3" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True" Height="140"
                  Background="{DynamicResource ContentBackground}"
                  Foreground="{DynamicResource PrimaryText}"
                  AlternatingRowBackground="{DynamicResource GridRowAlt}"
                  BorderBrush="{DynamicResource BorderColor}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="ProfileNameColumn" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn x:Name="ProfilePathsColumn" Binding="{Binding PathsDisplay}" Width="250"/>
                <DataGridTextColumn x:Name="ProfileProgramsColumn" Binding="{Binding ProgramsDisplay}" Width="200"/>
                <DataGridTextColumn x:Name="ProfileUpdatedColumn" Binding="{Binding UpdatedAt}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Separator -->
        <Border Grid.Row="4" Height="1" Margin="0,12,0,8"
                Background="{DynamicResource SeparatorColor}"/>

        <!-- Snapshot section header -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock x:Name="SnapshotsLabel" FontSize="14" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryText}" VerticalAlignment="Center"/>
            <Button x:Name="RollbackButton" MinWidth="80" Height="28" Margin="8,0,4,0"
                    Click="RollbackButton_Click"
                    Background="{DynamicResource ButtonDangerBackground}"
                    Foreground="{DynamicResource ButtonDangerForeground}" BorderThickness="0"/>
        </StackPanel>

        <!-- Snapshots DataGrid -->
        <DataGrid x:Name="SnapshotsGrid" Grid.Row="6" AutoGenerateColumns="False"
                  CanUserAddRows="False" IsReadOnly="True"
                  Background="{DynamicResource ContentBackground}"
                  Foreground="{DynamicResource PrimaryText}"
                  AlternatingRowBackground="{DynamicResource GridRowAlt}"
                  BorderBrush="{DynamicResource BorderColor}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}">
            <DataGrid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="SnapProfileColumn" Binding="{Binding ProfileName}" Width="120"/>
                <DataGridTextColumn x:Name="SnapActionColumn" Binding="{Binding Action}" Width="120"/>
                <DataGridTextColumn x:Name="SnapTimestampColumn" Binding="{Binding Timestamp}" Width="200"/>
                <DataGridTextColumn x:Name="SnapRollbackColumn" Binding="{Binding RollbackDisplay}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
